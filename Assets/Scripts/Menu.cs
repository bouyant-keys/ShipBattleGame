using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Menu : MonoBehaviour
{
    public enum MenuType { Default, Pause, Intro, Cleared};
    public MenuType _menuType;
}
